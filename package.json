{
  "name": "gen-ai-wrapper",
  "version": "1.0.0",
  "description": "Package for genrative output from the ai models like gemini, chatGPT etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-format": "prettier --check .",
    "format": "prettier --write .",
    "run": "npx ts-node src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.4.4"
  },
  "dependencies": {
    "@google/generative-ai": "^0.5.0",
    "openai": "^4.33.0"
  }
}
